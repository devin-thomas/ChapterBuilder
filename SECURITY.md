# Security policy

## Supported versions

Only the latest GitHub Release is supported.

## Reporting a vulnerability

Do not open a public issue for a vulnerability that could expose user files or allow arbitrary code execution. Contact the repository owner privately through GitHub instead.

Release binaries are currently unsigned. Verify downloaded release assets against `SHA256SUMS.txt` or with GitHub artifact attestations before running them.
